# Changesets

This directory is used by [Changesets](https://github.com/changesets/changesets) to track unreleased changes.

When contributing, run `npx changeset` to describe your change before opening a PR.
